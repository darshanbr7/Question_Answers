/*  how to empty the array in javascript */

let arr = [1, 2, 3, 4, 5];
// to make empty an array here assigning length = 0
arr.length = 1;

//or else assiging empty array to the existing array
 arr = []
console.log(arr);